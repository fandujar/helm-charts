apiVersion: v1
kind: Service
metadata:
  name: {{ include "fandujar-deployment.fullname" . }}
  labels:
    {{- include "fandujar-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fandujar-deployment.selectorLabels" . | nindent 4 }}
